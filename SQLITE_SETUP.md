# ✅ SQLite Backend Setup - جاهز للاستخدام!

## التطبيق دلوقتي بيستخدم SQLite على السيرفر

### المميزات:
- ✅ قاعدة بيانات واحدة على السيرفر
- ✅ كل الأجهزة بتشوف نفس البيانات
- ✅ بدون Docker
- ✅ بدون Supabase
- ✅ بدون تعقيدات

## كيفية التشغيل:

```bash
npm run dev
```

التطبيق هيشتغل على: `http://localhost:3001`

## البيانات محفوظة فين؟

في ملف SQLite على السيرفر بتاعك: `data/kstore.db`

## الوصول من أجهزة تانية:

### 1. على نفس الشبكة (Wi-Fi):

اعرف IP address بتاع جهازك:
```bash
hostname -I
# أو
ip addr show | grep "inet " | grep -v 127.0.0.1
```

افتح من أي جهاز: `http://YOUR_IP:3001`

### 2. عبر الإنترنت:

```bash
# استخدم ngrok
npx ngrok http 3001
```

هيديك URL زي `https://abc123.ngrok.io` - استخدمه من أي مكان!

## API Endpoints:

- **Products**: `/api/products` (GET, POST, PUT, DELETE)
- **Invoices**: `/api/invoices` (GET, POST, DELETE)
- **Settings**: `/api/settings` (GET, POST)
- **Stats**: `/api/stats` (GET)

## Backup البيانات:

البيانات كلها في ملف واحد: `data/kstore.db`

للـ backup، انسخ الملف ده في مكان آمن:
```bash
cp data/kstore.db backup/kstore-$(date +%Y%m%d).db
```

---

**دلوقتي كل الأجهزة هتشوف نفس البيانات! 🎉**
